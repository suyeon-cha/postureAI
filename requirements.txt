# All local. Nothing here reaches a hosted model API.
mediapipe
opencv-python
fastapi
uvicorn[standard]
websockets
numpy
piper-tts
requests
pyyaml

# Voice input (P1). Optional — the UI keeps its text box if this is absent.
faster-whisper
